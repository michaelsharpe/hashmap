<%= form_tag(user_collection_collection_tags_path(user.id, collection.id), method: "post",  remote: true,) do %>
  <%= text_field_tag 'collection_tags[name]', nil, placeholder: "Things, stuff, plants..." %>
  <%= submit_tag("Add") %>
<% end %>